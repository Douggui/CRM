api_platform:
  defaults:
    pagination_items_per_page: 5 
    pagination_client_enabled: true
  mapping:
    paths: ['%kernel.project_dir%/src/Entity']
  collection:
    pagination:
      enabled: false
      # enabled_parameter_name: pagination # optional
      # items_per_page_parameter_name: itemsPerPage # default
    
